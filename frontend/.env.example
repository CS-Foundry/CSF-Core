# Frontend Environment Variables

# API Base URL - used by SvelteKit at build time
# For production: /api (relative path, backend proxies)
# For development: http://localhost:8000/api (direct backend URL)
PUBLIC_API_BASE_URL=/api
